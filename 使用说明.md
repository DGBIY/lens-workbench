# 📖 镜头设计工作台（Lens Design Workbench）使用说明

> 版本：v0.14（自包含分享版） ｜ 最后更新：2026-08-19
>
> 基于 **Streamlit + optiland** 的交互式镜头设计工具。所有光学计算均用 optiland 光线追迹引擎完成，**不依赖 Zemax**，开箱即用。

---

## 目录

1. [项目简介](#1-项目简介)
2. [环境要求与安装](#2-环境要求与安装)
3. [启动与快速上手](#3-启动与快速上手)
4. [目录结构](#4-目录结构)
5. [界面总览](#5-界面总览)
6. [📊 数据页（精英载入）](#6--数据页精英载入)
7. [⚙️ 系统页（口径/视场/波长/物距）](#7--系统页口径视场波长物距)
8. [🛠 设计页（LDE 表格编辑）](#8--设计页lde-表格编辑)
9. [主区域视图（2D/3D/Spot/像差分析）](#9-主区域视图2d3dspot像差分析)
10. [⚡ 局部优化](#10--局部优化)
11. [🚀 运行页（GA 运行控制·可选）](#11--运行页ga-运行控制可选)
12. [数据文件与配置](#12-数据文件与配置)
13. [分享给别人 / 上传 GitHub](#13-分享给别人--上传-github)
14. [常见问题 FAQ](#14-常见问题-faq)
15. [自检与一致性验证](#15-自检与一致性验证)
16. [版本历史](#16-版本历史)

---

## 1. 项目简介

镜头设计工作台是面向**6 片式 APO 匹兹伐（Petzval）深空摄影镜头**设计流程的可视化工具，核心能力：

| 能力 | 说明 |
|---|---|
| **LDE 自由编辑** | 类 Zemax 表格：白纸作画，任意添加/删除面，玻璃名自动带出 AGF 实测 nd/vd |
| **真实光线追迹** | optiland 引擎：多波长（F/d/C）多视场真实追迹，与 Zemax 结果一致（EFFL/AXCL/RSCE 已实测对齐） |
| **全面分析** | 2D/3D Layout、点列图（含艾里斑圈）、场曲/畸变/光线扇形图/RMS/包围能量/MTF/波前/透过焦等 10 种 + 轴向色差 |
| **局部优化** | Nelder-Mead 多变量优化（空气间隔+后焦，可选厚度/曲率），MFE 分解预览 + 收敛曲线 |
| **玻璃搜索** | 内置 81 种 CDGM/SCHOTT 玻璃 AGF 实测数据，按 nd/vd 秒搜最接近玻璃 |
| **导出** | .zmx（Zemax 格式）/ LDE 文本 / 分析图 PNG 一键下载 |

设计目标规格（本工具针对的场景）：**200mm F/5（40mm 口径）**，半视场角 5.8°，全画幅，APO 色差（AXCL）+ 平场（场曲）+ 星点（RSCE）。

---

## 2. 环境要求与安装

### 系统要求
- Windows 10/11（开发环境）
- Python **3.10 或更高**
- 不需要安装 Zemax（仅"🚀 运行页"的 GA 任务控制是可选扩展，才需要完整项目环境）

### 安装依赖
```powershell
cd C:\Users\Administrator\Desktop\GA\窗口
pip install -r requirements.txt
```

`requirements.txt` 内容：streamlit、optiland、numpy、pandas、matplotlib、scipy。

> 💡 建议使用独立虚拟环境：`python -m venv .venv` 后激活再安装。

---

## 3. 启动与快速上手

### 启动
```powershell
cd C:\Users\Administrator\Desktop\GA\窗口
streamlit run app.py
```
浏览器自动打开 http://localhost:8501

> 💡 **一键启动**：双击文件夹里的 **《启动工作台.bat》** 即可（自动开浏览器；关闭黑色窗口＝停止工作台）。

### 三步上手
1. **启动后默认已加载**精英55 样例结构（若 `data/samples/` 或 GA results 中无对应文件，自动使用内置默认结构），顶部指标行显示 EFFL≈200mm。
2. 左侧栏切到 **🛠 设计** → 表格里改厚度/玻璃，或点 **✨ 新建空白镜头** 白纸作画。
3. 主区域顶部切换 **2D Layout / 3D Layout / Spot / 像差分析** 视图，边看边改。

---

## 4. 目录结构

```
窗口/                          ← 工作台根目录（可整个拷走分享）
├── app.py                    # 主程序（Streamlit 页面）
├── config.py                 # 集中配置（数据路径/版本/兼容常量）
├── README.md                 # 项目简介（英文技术向）
├── 使用说明.md                # 本文档
├── requirements.txt          # 依赖清单
├── LICENSE                   # MIT 许可证
├── .gitignore                # Git 忽略规则
├── core/                     # 核心代码
│   ├── lens_io.py            # LDE/规格/精英解析/导出（含自包含精英解析器）
│   ├── layout2d.py           # 2D Layout 绘制
│   ├── layout3d.py           # 3D Layout 绘制
│   ├── spot_rms.py           # 点列图 + RMS + 艾里斑圈
│   ├── analysis.py           # 10 种像差分析 + MTF + 轴向色差 + 优化器 + 玻璃搜索
│   ├── eval.py               # 指标评估（EFFL/AXCL/RSCE/总长）
│   ├── run_control.py        # GA 运行控制（可选功能）
│   ├── compare.py            # 多精英对比
│   ├── _bridge.py            # 自包含桥接（光学核心函数，无外部依赖）
│   ├── _library.py           # 镜片库加载器（读 data/pybl）
│   └── _glass_table.py       # 内置玻璃表（81 种 AGF 实测 nd/vd）
└── data/                     # 数据目录（可整体迁移）
    ├── pybl/                 # 镜片库数据 pybl1-6.txt（GA 精英解码用）
    └── samples/              # 样例/用户精英文件（可选，.gitignore 已排除）
```

---

## 5. 界面总览

### 左侧栏（导航）

| 分段 | 内容 |
|---|---|
| 📊 **数据** | 载入精英、重置默认结构、当前结构来源标签 |
| ⚙️ **系统** | EPD、视场类型/场列表、波长、主波长、物距 |
| 🚀 **运行** | GA 任务控制（可选）、运行状态、收敛曲线、任务日志 |
| 🛠 **设计** | LDE 表格 + 画布管理、玻璃目录/搜索、多精英对比 |

### 主区域

- **指标行（6 个卡片）**：近轴 EFFL ｜ AXCL（轴向色差）｜ 总长 ｜ RSCE（点列图 RMS）｜ F/# ｜ 后焦 BFL
- **导出按钮**：📤 导出 .zmx（Zemax 格式）｜ 📄 LDE 文本
- **视图切换**（分段控件）：2D Layout ｜ 3D Layout ｜ Spot Diagram ｜ 像差分析
- **页脚状态栏**：口径 | F/# | 波长 | 视场 | 主波长 λ | 视场类型 | 物距 | 面数 | 总长

---

## 6. 📊 数据页（精英载入）

> 侧边栏 → 📊 数据

**精英文件**是 GA 搜索结果文件（格式见 [12 节](#12-数据文件与配置)），每个精英代表一种镜片组合（6 个镜片位置：类型+库行号，5 个空气间隔）。

| 控件 | 说明 |
|---|---|
| 精英文件 | 文件名（相对精英目录）。**配置了 GA_PROJECT_ROOT** 时读取 `<GA项目>/results/` 下的文件；未配置时读 `data/samples/` |
| 精英序号 | 载入第几个精英（1 = 最优） |
| **载入精英** | 载入并重建 LDE 表格 |
| **重置为默认** | 恢复内置默认结构（精英55 的等效结构） |

> 启动时默认自动载入 `run_20260818_213729/ga_elite.txt` 第 55 个精英；找不到文件时自动使用内置默认结构，不影响使用。

---

## 7. ⚙️ 系统页（口径/视场/波长/物距）

> 侧边栏 → ⚙️ 系统

| 控件 | 默认 | 说明 |
|---|---|---|
| **EPD（入瞳直径 mm）** | 40.0 | 镜头口径；改动后所有追迹按新口径计算 |
| **视场类型** | angle | `angle`=角度视场（度）/ `object`=物高视场（mm） |
| **视场列表** | 0/2/4.06/5.8° | 逗号分隔，回车生效；用于所有分析 |
| **波长** | F/d/C 三色 | 486.13 / 587.56 / 656.27 nm（勾选启用） |
| **主波长** | 第 1 个 | 下拉选择；用于艾里斑计算等单波长指标 |
| **物距** | ∞（无穷远） | 留空 = 无穷远；输入数值 = 有限共轭（mm） |

> 改动会触发全部分析图自动重算（带缓存，切换回来即时显示）。

---

## 8. 🛠 设计页（LDE 表格编辑）

> 侧边栏 → 🛠 设计（页内有 3 个标签页：LDE / 玻璃 / 对比）

### 8.1 LDE 表格

类 Zemax 的镜片数据表，**直接在单元格里改，回车即生效**：

| 列 | 说明 |
|---|---|
| Surf | 面序号（0=物面，1=光阑，末面=像面） |
| Radius (R) | 曲率半径（mm），**正=球心在右侧**，inf=平面 |
| Thick (t) | 厚度（mm），正=下一面在右侧；物面=物距，像面前=后焦 |
| Glass | 玻璃名（**输入已有玻璃名自动带出 nd/vd**，留空=空气） |
| ND / VD | 玻璃折射率 / 阿贝数（玻璃名已存在时自动填充，可手改） |
| Semi-Dia | 半口径（mm）；**0 = 光阑位置**（只能有一个，改动后自动迁移） |
| 备注 | 任意注释 |

**行操作**：
- **删除行**：勾选要删的行 → 点"🗑 删除选中行"（自动避开光阑/物面/像面）
- **恢复默认结构**：一键回到内置默认结构

**画布管理**（设计页顶部）：
- **✨ 新建空白镜头**：白纸作画（物面+光阑+像面三行起步，其余自己加）
- **重置默认**：回到内置默认结构

**玻璃名 → nd/vd 规则**：表格里直接输入 `BAF4`、`H-FK61`、`ZF6` 等已知玻璃名，ND/VD 列自动填入 AGF 实测值（内置 81 种表 + 库文件兜底）。输入未知玻璃名则 ND/VD 留空需手填。

### 8.2 添加镜片：手动定义 / 从镜片库挑选

设计页"➕ 添加镜片"区有两种来源（顶部"镜片来源"切换）：

**手动定义**（默认）：选玻璃（内置 81 种自动带出 nd/vd）+ 填 R1/R2/厚度/后空气/半口径 → 插入到指定面后。

**从镜片库挑选**（推荐）：
1. **库类型**下拉：库1-4 = 单镜片库、库5-6 = 成品双胶合库
2. **行号**输入：范围自动显示该库的有效行区间（如库1 有效 202-400）——只填有效区间即可
3. **实时预览**：🔍 显示该条目的完整参数（玻璃/nd/vd/R1/R2/厚度；双胶合显示两片玻璃）
4. 填**后空气 / 半口径** → **插入库镜片**：单镜片插入 2 行、双胶合插入 3 行（胶合面无空气间隔，与精英解码/optiland 构建完全一致）

> 库数据即 GA 精英解码用的同款镜片库（data/pybl/），插入后与 GA 方案完全兼容，可继续做局部优化。

### 8.3 玻璃标签页

- **玻璃目录**：内置 81 种玻璃一览表（名称 + nd + vd）
- **🔎 玻璃搜索**：输入目标 nd / vd → 点"搜索"→ 按接近度排序的结果表（距离 = 归一化欧氏距离，越小越接近），例如搜 D-K9（1.5163, 64.1）返回 D-K9、H-K9L、BK7 等。

### 8.4 对比标签页

多精英对比：输入精英文件 + 序号列表（逗号分隔）→ 载入对比 → 表格对比各精英的 EFFL / AXCL / RSCE / 总长 / 结构面数，方便挑选候选。

---

### 8.5 镜头样板（经典 / 现代 / 常用构型）

设计页 → "📚 镜头样板"展开区——一键载入经典镜头构型：

| 构型 | 说明 |
|---|---|
| 匹兹伐 Petzval / 双高斯 Double Gauss / 天塞 Tessar / 松纳 Sonnar / 库克三片 Cooke / 望远 Telephoto / 反望远 Retrofocus / 消色差双胶合 Achromat / 海利亚 Heliar / 达戈 Dagor | 10 个经典/现代/常用构型 |
| **APO 复消色差物镜**（三片式） | ED 双胶合（FK61 超低色散 + 火石）+ 单片冕——深空摄影主力（🔭 折射） |
| **牛顿 Newtonian / 卡塞格林 Cassegrain / 里奇-克雷蒂安 RC / 施密特-卡塞格林 SCT / 马克苏托夫 Maksutov** | 反射/折反射望远镜（🪞 反射构型仅支持"② 完全复刻"；LDE 玻璃列填 MIRROR 可手建反射面） |

> 🪞 反射模板说明：反射面后厚度为**负值**（Zemax 惯例，折返光路）；像面在主镜前（无主镜穿孔简化模型）；顶部"近轴 EFFL"对反射系统显示"—"（paraxial 引擎不适用于反射），焦距以真实追迹聚焦为准（卡塞格林类 ≈0.96×目标焦距）；SCT 校正板 A4 系数可用 ⚡ 优化微调。

两种生成模式（radio 切换）：
- **① 镜片库凑**：按槽位（单片/双胶合 + 光焦度符号 + 最小口径）从镜片库随机匹配——结构近似经典构型，**每次生成不同组合**（可反复点击换组合）
- **② 完全复刻**：按样板示例参数直接生成（f=100mm 基准自动缩放 + 玻璃按 nd/vd 自动匹配内置 81 种 AGF 表）——参数为经典结构示意值（结构/玻璃类型正确），生成后可配合 ⚡ 优化微调

可调：目标焦距 / 后焦 / 口径 EPD。生成后载入主编辑区（LDE 实时刷新），建议用 ⚡ 优化 + 模拟星场验证像质。

## 9. 主区域视图（2D/3D/Spot/像差分析）

### 9.1 2D Layout
- 三波长光线（F 蓝 / d 绿 / C 红）+ 每视场一束光线
- 后焦用黑色 X 标记焦点位置
- 适合快速检查光路走向、后焦是否在像面

### 9.2 3D Layout
- 镜片旋转体半剖面 + 光线（三波长着色）
- 鼠标拖拽旋转视角，看整体结构

### 9.3 Spot Diagram（点列图）
- 每个视场一个子图，三波长光线落点
- **黑色虚线圆 = 艾里斑**（1.22λF#，Zemax 同款标注）——RMS 小于艾里斑即接近衍射极限
- 子图标题显示该场 RMS（全波长最大）
- 指标行的 RSCE = 所有视场中最大的相对质心 RMS（µm）

### 9.4 像差分析（选择类型**自动计算**，无需点按钮）

| 类型 | 用途 |
|---|---|
| 场曲 Field Curvature | 子午/弧矢场曲 + 畸变曲线（T 蓝 / S 红） |
| 畸变 Distortion | 畸变百分比 vs 视场 |
| 光线扇形图 Ray Fan | 各视场光线像差（横=入瞳坐标，纵=像差） |
| RMS vs 视场 | 点列图 RMS 随视场变化 |
| 能量集中度 Encircled Energy | 各视场包围能量 vs 半径（衍射极限参考线） |
| 畸变网格 Grid Distortion | 网格变形直观显示 |
| 透过焦 Spot | 对焦敏感度（不同离焦量的点列大小） |
| 透过焦 MTF | 对焦位置对 MTF 的影响 |
| RMS 波前误差 vs 场 | 波前质量（越小越接近衍射极限） |
| MTF vs 空间频率 | 星点锐度（各视场/波长），深空镜头核心指标 |
| 🌈 轴向色差（APO，独立展开区） | F/d/C 三波长焦点分离曲线——APO 判定 |

每张图下都有 **⬇ 下载 PNG** 按钮。

> 所有分析带缓存：切换回同一类型即时显示，不重复计算。

### 9.5 总览视图（2×2 多窗口 · Zemax 平铺风格）

分析视图切换到 **总览**：四个分析窗口**并行平铺**（2D Layout / Spot / MTF / 场曲），一眼看全像质——Zemax 多窗口工作台的感觉。

### 9.6 窗口大小可调

| 方式 | 说明 |
|---|---|
| **拖拽缩放** | 每个分析图窗口**右下角可拖拽缩放**（灰色边框窗口，像 Zemax 的分析窗口） |
| **图窗口尺寸滑块** | 侧边栏 ⚙️ 系统页 → "图窗口尺寸"（0.6×-1.6×）：所有分析图整体缩放 |
| **表格高度滑块** | LDE 表格上方"表格高度 (px)"：320-900px 可调 |
| **列宽拖拽** | LDE 表格列分隔线可直接拖拽调列宽（原生支持） |

### 9.7 🔭 天文工具（深空摄影计算）

主区域第 4 个 tab，三个区块（数值与当前镜头实时联动）：

**📷 成像系统计算器**：选传感器（全画幅/APS-C/M4/3/1″）+ 像元尺寸（默认 3.76µm）+ 焦距（默认当前镜头）→ 8 项实时输出：
- 像元尺度（″/px = 206.265×像元µm/焦距）
- 视场角（宽×高，度）
- 衍射极限（138.4/D ″）
- 艾里斑直径（2.44λF#，µm）
- 星点 FWHM（≈2.355×RSCE，″）
- 采样率判定（FWHM/像元：<1.5 欠采样警告、>4 过采样建议减焦、1.5-4 Nyquist 合理）
- 极限星等（人眼 7mm 基准 6.5 等近似）、集光力（×人眼）

**🔭 星点 / 视宁度 / 焦深**：视宁度滑块（seeing ″）对比衍射极限——判断口径是否受视宁度限制；临界焦深 ±2λF#²（对焦精度需求）；铝筒温漂 α·L·ΔT（超过焦深提醒电动调焦/温补）。

**🛠 配件计算**：
- 滤镜/平窗焦点位移 t(1-1/n)（装滤镜后焦点后移量，需重新对焦）
- 减焦/增倍合成系统（合成焦距、F#、FOV 倍率）
- 相机卡口法兰距匹配（天文 T 环 55 / 佳能 EF 44 / 索尼 E 18 / 尼康 Z 16 / 佳能 RF 20mm）——与当前镜头后焦 BFL 对比，提示直连或需转接环

**🪐 月面 / 行星像比例**（v0.19）：天体预设（月亮 31′/太阳 32′/木星 40″/土星 18″/火星 17″/金星 12″）+ 焦距 → 像直径（mm = f·tan 角直径）、占像素数、占画幅宽 %——规划增倍镜 / 行星相机像元用。

**🌌 曝光 / 极限星等（SNR 模型）**（v0.19）：550nm 宽带光子统计（SNR = S/√(S+天光+读出²+暗电流·t)）——输入目标星等/单张曝光/QE/天光（暗空 21.5 → 城市 17.0）/读出噪声/暗电流/叠加张数 → 输出目标 SNR、单张极限星等（SNR=10）、N 张叠加极限（+2.5log√N）、SNR 达标判定（<10 欠曝警告 / 10-50 建议叠加 / ≥50 充足）。

**🔧 平场镜（减焦镜）薄透镜设计**（v0.19）：薄透镜组合公式 1/f=1/f₁+1/f₂−d/(f₁f₂)——正向：主镜+减焦镜（f₂、间距 d）→ 合成焦距/减焦率；反向：目标减焦率 → 反推所需 f₂（设计规格，真实减焦镜为双胶合组，先定规格再用 LDE 设计验证）。

**🌠 模拟星场（像差可视化）**（v0.21）：把当前镜头当望远镜，真实光线追迹渲染模拟星空——**每颗星 = 该视场点列图**（三波长 F/d/C 叠加）：
- 网格演示（5×5 视场）：中心圆点=球差/色差、边缘彗星尾=彗差、四角椭圆=像散、星点彩边=横向色差，全视场像差分布一目了然
- 随机星空：随机位置+亮度（8-17 等），深空照片感
- 像差放大滑块（3-60×）+ 离焦滑块（±500µm，模拟不同调焦位置）+ RMS 标注开关
- 🎞 离焦对比：5 帧并排（-400/-200/0/+200/+400µm）——对焦判断
- 🎯 测算最佳对焦：扫描 ±0.4mm 中心+边缘星总 RMS → 最佳像面偏移

**📷 曝光模拟照片（单张 vs 叠加）**（v0.21）：像素级噪声模拟——每颗星真实追迹 RMS → 高斯 PSF，噪声=天光+读出+暗电流；并排输出"单张原始"（暗星淹没在噪声里）vs"叠加 N 张"（噪声降 √N、暗星浮现），标题直接标注单张/叠加极限星等。整幅 / 中心 3× 切换。

### 9.8 非球面支持（EVENASPH）

LDE 表格新增 4 列（仅玻璃面可编辑，v0.21）：
- **Conic**：圆锥常数 k（0=球面；抛物面 -1、椭球面 0>k>-1、双曲面 k<-1）
- **A4 / A6 / A8**：偶次非球面 ρ⁴/ρ⁶/ρ⁸ 系数（Zemax A4/A6/A8 命名；任意一个非空即启用非球面）

输入系数后 → optiland 用 EvenAsphere 面型真实追迹（与 Zemax EVENASPH 公式一致：z = cρ²/(1+√(1-(1+k)c²ρ²)) + ΣAᵢρ^(2i)）——2D/Spot/星场/优化全部自动生效；导出 .zmx 时自动写 EVENASPH 面行+系数行（Zemax 打开后请核对参数顺序）。**注意**：非球面系数影响高阶像差（如球差），不影响近轴焦距——改系数后看 Spot/星场验证效果。

---

## 10. ⚡ 局部优化

> 设计页 → "⚡ 优化（局部）"展开区

- **当前 MFE 分解预览**：优化前直接显示当前 EFFL（目标 200）/ AXCL（目标 0.05）/ RSCE，一目了然差距
- **快速模式**（勾选）：近轴优化（不含 RSCE），约 10 秒
- **标准模式**（默认）：含星点 RSCE，1-3 分钟
- **包含镜片厚度**（勾选）：玻璃面厚度也作为变量（默认只动空气间隔 + 后焦）
- **▶ 运行优化**：Nelder-Mead 多起点搜索，进度条 + 收敛曲线实时显示，完成后指标行自动刷新
- 结果可再配合 2D/Spot 视图检查，满意后可导出 .zmx

---

## 11. 🚀 运行页（GA 运行控制·可选）

> 侧边栏 → 🚀 运行。**可选功能**：未配置 GA 环境时显示提示，其余功能不受影响。

启用方式（见 [12 节](#12-数据文件与配置)）：设置环境变量 `GA_PROJECT_ROOT` 指向完整项目后重启。

启用后可用任务（8 个）：

| 任务 | 说明 |
|---|---|
| 三层漏斗流水线（GPU GA→精筛→解码） | 完整 GA 优化（quick>0 为快速模式，代数=50） |
| 完整流水线（GPU 全套） | 验证→GA→解码全流程 |
| 解码 | 精英 → 镜片参数 |
| 精修优化 | 对精英做局部精修（Nelder-Mead） |
| 场曲精修 / 色差精修 | 专项修复场曲 / 色差 |
| Zemax 验证 | 用 OpticStudio COM 复算关键指标（**注意：会启动 Zemax，勿与其他 Zemax 任务并发**） |
| 生成可视化 PNG | 输出结构图/点列图/场曲 PNG |

操作：选任务 → 填参数（如有）→ **▶ 启动** / **⏹ 停止**。

**运行状态（v0.22 图表化）**：
- 侧边栏"📊 运行状态"：状态 / PID / 已运行时长（分秒）/ 收敛数据点 / 最新 best + 迷你收敛曲线
- 主区"📈 GA 收敛 + 运行"tab：5 项状态指标卡（状态/PID/已运行/数据点/best）+ 收敛曲线（best + 最优保持 + 5 代平滑三线）+ 相对首代提升倍数 + 最近 20 代明细表；每 5 秒自动刷新；日志进度自动解析（第 N 代）

**🧬 optiland GA（v0.24，零 Zemax 依赖）**——侧边栏"🚀 运行"→"🧬 optiland GA"展开区：
- **评估引擎开关**（v0.25）：① 近轴 GA · CPU 向量化（6 组，默认，500×20 代 ≈ 1s）② 近轴 GA · GPU（6 组，需 N 卡 CUDA + python_env，50000×200 代 ≈ 2 分钟）③ 内置 optiland（任意组数 4-10，全指标慢）。近轴引擎**复用完整项目 ga_fast**（KNN 突变/重启/多样性/爬坡/去重，子进程隔离调用——避免 config 冲突 + 挂起可 kill + 进度实时轮询）；需要 Zemax 时用上方"▶ 任务控制"（8 个任务保留）
- **两阶段精修**（v0.25）：GA 跑完默认自动接 Nelder-Mead 快速精修（空气间隔/后焦），改善才采用；精修后可关
- **镜片组数**：内置模式 4-10 可配；近轴引擎固定 6 组（匹配精英格式，选非 6 组自动提示）
- **评价函数**：12 种操作数（EFFL/AXCL/FCGT/FCGS/RSCE/DIST/WFE/TOTR/BFL/MTF/THF/RELIL）× 预设 + 权重；MFE = √(Σw·Δ²/Σw)（Zemax 惯例）
- **模板种子**（内置模式）：样板构型生成初始个体
- **跑完自动回填**：best 精英自动载入 LDE + EFFL/AXCL/RSCE + 星场验证图 + 收敛曲线
- 一致性验证：GPU 近轴 vs optiland 对同一精英 EFFL 差 0.02%（200.212 vs 200.1645），AXCL 同量级——近轴引擎结果可安全精筛

> ⚠️ 锁机制：同一时间只允许一个任务（results/.panel_running 锁），防 GPU 并发冲突。

---

## 12. 数据文件与配置

### 12.1 精英文件格式

每 3 行为一个精英块（UTF-8 文本）：

```
精英1: EFFL=200.1 AXCL=0.112 Petz=-0.05e-3 MFE=0.02
  镜片: [(5, 259), (5, 191), (5, 151), (4, 231), (2, 244), (2, 199)]
  空气: ['35.75', '30.75', '26.84', '9.84', '5.92']
```

- `镜片`：6 组 `(镜片类型, 库行号)`；`空气`：5 个空气间隔（mm）
- 文件里可放任意多个精英块（自动按文件顺序编号）
- 分享版把文件放到 `data/samples/` 即可在"📊 数据"页载入

### 12.2 环境变量

| 变量 | 默认 | 说明 |
|---|---|---|
| `WORKBENCH_DATA` | `./data` | 数据目录（镜片库/样例） |
| `GA_PROJECT_ROOT` | 未设置 | GA 完整项目根（含 python_env/ scripts/ results/）——设置后启用运行页任务 + 精英文件目录改为其 results/ |

PowerShell 设置示例：
```powershell
$env:GA_PROJECT_ROOT = "C:\Users\Administrator\Desktop\GA\完整项目"
streamlit run app.py
```

### 12.3 内置数据

- **玻璃表**（core/_glass_table.py）：81 种 CDGM/SCHOTT 玻璃 AGF 实测 nd/vd，零外部依赖
- **镜片库**（data/pybl/）：pybl1-6.txt，共 6 个库类型（GA 基因 T1-T6 一一对应）：
  - **库1-4 = 单镜片库**：有效候选 131/238/459/238 个（有效行区间：库1 202-400、库2 125-416、库3 369-981、库4 125-416）
  - **库5-6 = 成品双胶合库**：各 181 个（有效行区间 51-259 / 52-259），直接带胶合约束
  - 合计 1,428 个有效候选；"从镜片库挑选"功能按有效区间选行即可
- **样例**（data/samples/）：可放自己的精英文件（Git 忽略，不上传）

---

## 13. 分享给别人 / 上传 GitHub

### 分享给别人
整个 `窗口/` 文件夹拷走即可，对方只需：
```bash
pip install -r requirements.txt
streamlit run app.py
```
**无需**：Zemax、完整项目、任何环境变量（GA 运行控制自动降级为"未配置"提示）。

### 上传 GitHub（5 步）
```bash
cd C:\Users\Administrator\Desktop\GA\窗口
git init
git add .
git commit -m "镜头设计工作台 v0.14：自包含 Streamlit 光学设计工具"
# 方式A：用 gh CLI 一步创建并推送
gh repo create lens-workbench --public --source . --push
# 方式B：先在 github.com 建空仓库，再：
git remote add origin https://github.com/<你的用户名>/lens-workbench.git
git branch -M main
git push -u origin main
```

发布前建议：
1. 截 2-3 张界面图放进 README（主界面 / LDE / Spot 或 MTF）
2. LICENSE 的 copyright 改成你的名字
3. 想好是否附带一个样例精英文件（当前 .gitignore 已排除 data/samples/*，保持干净）

---

## 14. 常见问题 FAQ

| 问题 | 处理 |
|---|---|
| **启动后页面报错/空白** | 检查端口 8501 是否被占用（换 `--server.port`）；看终端报错是否为 `pip install -r requirements.txt` 后仍缺包 |
| **载入精英后表格没变** | 文件路径不对或格式不符（见 12.1）；未配置 GA_PROJECT_ROOT 时文件要放 `data/samples/` |
| **玻璃名输入后 nd/vd 没自动填** | 该玻璃不在内置表——直接手填 ND/VD 即可 |
| **运行页显示"GA 运行控制未配置"** | 正常——可选功能；按 12.2 设置 `GA_PROJECT_ROOT` 后重启 |
| **Spot 图里艾里斑是虚线圆吗** | 是的，黑色虚线圆 = 艾里斑（1.22λF#） |
| **优化太慢** | 勾选"快速模式（近轴）"约 10 秒；标准模式含星点 1-3 分钟属正常 |
| **终端有 µ 字形字体警告** | 无碍（matplotlib 字体缺字形），不影响计算与输出 |
| **MTF/EE 曲线计算慢** | 大光线数追迹正常耗时，属分析类计算，首次 3-10 秒，之后缓存即时 |
| **修改了核心代码想确认没改坏** | 运行自检（见第 15 节） |

---

## 15. 自检与一致性验证

```powershell
python core/lens_io.py       # LDE/规格往返 + RSCE 新旧路径一致
python core/eval.py          # 指标评估一致性（EFFL/AXCL）
python core/analysis.py      # 10 种分析 + 玻璃搜索
python core/compare.py       # 多精英对比表
```

自检覆盖（v0.14 实测全通过）：
- 精英55 解码 → 18 面，EFFL=200.05mm，AXCL=+0.086，RSCE=474µm（与 Zemax 结果一致）
- 内置玻璃表 81 种；玻璃搜索 D-K9 命中
- 分享环境（不配置任何环境变量）全功能可用

---

## 16. 版本历史

| 版本 | 内容 |
|---|---|
| **v0.25**（当前） | **GPU/向量化近轴 GA 接入 + 两阶段精修**：评估引擎三选（近轴 CPU 向量化 500×20 代≈1s / 近轴 GPU 需 N 卡 50000×200 代≈2min / 内置 optiland 任意组数）；**复用完整项目 ga_fast**（KNN 突变/重启/爬坡/去重，子进程隔离调用避免 config 冲突 + 挂起可 kill + 进度轮询）；GA 跑完自动 Nelder-Mead 精修；双引擎一致性验证 EFFL 差 0.02% |
| **v0.24** | **optiland 版 GA**（零 Zemax）：🧬 运行页内置遗传算法（组数 4-10 可配置、模板种子联动、锦标赛 k=8、超时淘汰防护、收敛日志兼容图表）；**评价函数系统** core/merit.py（12 种操作数 EFFL/AXCL/FCGT/FCGS/RSCE/DIST/WFE/TOTR/BFL/MTF/THF/RELIL，预设+权重，MFE=√(ΣwΔ²/Σw)，精英55 对照 MFE 0.377 vs Zemax 0.259 同量级）；**跑完自动回填 LDE + 星场验证**；**深空目标模拟**（曝光模拟叠加合成 M42 星云）；Zemax 引擎保留（运行页"▶ 任务控制"开关） |
| **v0.23** | 天文模板专列：🔭 APO 复消色差物镜（三片式）+ 🪞 5 个反射构型（牛顿/卡塞格林/RC/SCT/马克苏托夫）——LDE 支持 MIRROR 反射面（含 conic/非球面系数），反射系统真实追迹聚焦验证（CAS/RC rms<0.01mm），负厚度折返建模；顶部 EFFL 对反射系统标注"—" |
| **v0.22** | 📚 镜头样板库（10 个经典/现代/常用构型：匹兹伐/双高斯/天塞/松纳/库克/望远/反望远/消色差/海利亚/达戈；① 镜片库凑随机组合 ② 完全复刻 f=100 基准缩放+玻璃自动匹配）；运行状态图表化（状态卡/已运行时长/三线收敛曲线/最近 20 代明细/侧边栏迷你图）；elite_to_specs 支持任意组数 |
| **v0.21** | 天文工具扩展 + 非球面：🪐 月面/行星像比例、🌌 曝光/极限星等（SNR 光子统计模型）、🔧 平场镜（减焦镜）薄透镜设计（正反向）、🌠 模拟星场（真实追迹像差可视化：网格/随机/离焦对比/最佳对焦/RMS 标注）、📷 曝光模拟照片（单张 vs 叠加，像素级噪声）；LDE 新增 Conic/A4/A6/A8 非球面列（EVENASPH，optiland EvenAsphere 真实追迹 + .zmx 导出） |
| v0.17 | Zemax 风格 UI 增强（分析图窗口可拖拽缩放、指标卡片化、侧边栏分区紧凑化） |
| v0.16 | 自包含版重构：core/ 模块化（lens_io/layout2d/layout3d/spot_rms/eval/analysis/astro_tools）、天文工具 tab 上线 |
| **v0.14** | 自包含分享版：内置玻璃表/镜片库/精英解析器，零外部依赖；GA 运行控制可选化；指标 6 列（F/#、后焦）、状态栏增强、像差自动计算、优化 MFE 分解预览、Spot 艾里斑圈；README/requirements/LICENSE/.gitignore |
| v0.12 | 3D Layout、物距（有限共轭）、视场类型（angle/object）、主波长选择、分析图下载 PNG、优化进度曲线 |
| v0.11 | 像差分析扩展到 10 种 + 轴向色差（APO）、MTF、玻璃搜索、多精英对比、局部优化（Nelder-Mead）、收敛曲线 |
| v0.10 | 工作台架构成型：LDE 表格编辑、2D/Spot、场曲/畸变、EFFL/AXCL/RSCE 指标 |

---

*遇到问题或想提功能，随时问开发助手；本工具用于设计参考，生产前请与供应商确认最终光学参数。*
